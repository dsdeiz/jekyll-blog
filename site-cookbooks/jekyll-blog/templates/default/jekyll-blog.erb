server {
  listen   80;
  server_name  jekyll.vagrant;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
    root   /var/www/current;
    index  index.html index.htm;
  }
}
